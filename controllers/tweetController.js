const { Tweet } = require("../models/Tweet");

const tweetController = {
  index: (req, res) => {},
  store: (req, res) => {},
  update: (req, res) => {},
  destroy: (req, res) => {},
};

module.exports = tweetController;
